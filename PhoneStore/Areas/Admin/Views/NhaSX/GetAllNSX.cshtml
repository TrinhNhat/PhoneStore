@model List<PhoneStore.Models.NHASANXUAT>
@{
    ViewBag.Title = "Quản lý nhà sản xuất";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section title {
    Quản lý nhà sản xuất
}

<br />
<div class="row">
    <div class="col-md-10 col-md-offset-1">
        <a href="/Admin/NhaSX/AddNSX" role="button" class="btn btn-primary">
            <i class="fa fa-plus"></i>&nbsp;Thêm nhà sản xuất
        </a>
    </div>
</div>
<div class="=row">
    <div class="col-md-10 col-md-offset-1">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th class="col-sm-2">Mã nhà sản xuất</th>
                    <th class="col-sm-3">Tên nhà sản xuất</th>
                    <th class="col-sm-3">Quốc gia</th>
                    <th class="col-sm-3">&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var sp in Model)
                {
                    <tr>
                        <td>@sp.MaNSX</td>
                        <td>@sp.TenNSX</td>
                        <td>@sp.QuocGia</td>
                        <td>
                            <a class="btn btn-danger btn-xs" href="@Url.Action("RemoveNSX", "NhaSX", new { id = sp.MaNSX })" role="button" title="Xóa">
                                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                            </a>
                            <a class="btn btn-primary btn-xs" href="@Url.Action("EditNSX", "NhaSX", new { id = sp.MaNSX })" role="button" title="Cập nhật">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <nav>
            <ul class="pagination">
                <li class="@(ViewBag.CurPage == 1 ? "disabled" : "")">
                    <a href="@(ViewBag.CurPage == 1 ? "#" : @Url.Action("GetAllNSX", "NhaSX", new { page = ViewBag.CurPage - 1 }))" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
                @for (int i = 1; i <= ViewBag.PageCount; i++)
                {
                    <li class="@(ViewBag.CurPage == i ? "active" : "")"><a href="@Url.Action("GetAllNSX", "NhaSX", new { page = i })">@i</a></li>
                }
                <li class="@(ViewBag.CurPage == ViewBag.PageCount ? "disabled" : "")">
                    <a href="@(ViewBag.CurPage == ViewBag.PageCount ? "#" : @Url.Action("GetAllNSX", "NhaSX", new { page = ViewBag.CurPage + 1 }))" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</div>

